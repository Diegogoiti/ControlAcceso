<Window x:Class="ControlAcceso.ReportesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Centro de Reportes — Dashboard" Height="700" Width="1050"
        MinHeight="620" MinWidth="900"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True">

    <Window.Resources>
        <!-- Botones de acción tipo píldora (igual que el panel de administración) -->
        <Style x:Key="DashButton" TargetType="Button">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" TargetName="border" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" TargetName="border" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" TargetName="border" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ============ CABECERA ============ -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="📊 Dashboard de Asistencia" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                <TextBlock x:Name="lblFechaHoy" Text="" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Semana:" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpFechaSemana" Height="36" Width="135" FontSize="13" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button x:Name="btnActualizar" Content="🔄 Actualizar" Style="{StaticResource DashButton}" Background="#E2E8F0" Foreground="#334155" BorderBrush="#CBD5E1" Click="btnActualizar_Click"/>
                    <Button x:Name="btnExportarSemana" Content="📄 Exportar semana (PDF)" Style="{StaticResource DashButton}" Background="#10B981" Foreground="White" BorderBrush="#059669" Click="btnExportarSemana_Click"/>
                    <Button x:Name="btnCerrar" Content="Cerrar" Style="{StaticResource DashButton}" Background="#FEE2E2" Foreground="#DC2626" BorderBrush="#FCA5A5" Margin="0" Click="btnCerrar_Click"/>
                </StackPanel>
                <TextBlock x:Name="txtMensajeExport" Text="" FontSize="12" Foreground="#DC2626" Margin="0,8,0,0" TextWrapping="Wrap" TextAlignment="Right" Visibility="Collapsed"/>
            </StackPanel>
        </Grid>

        <!-- ============ TARJETAS DE ESTADÍSTICAS ============ -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Empleados activos -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="16" BorderBrush="#E2E8F0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <Border Width="46" Height="46" CornerRadius="23" Background="#F1F5F9" VerticalAlignment="Center">
                        <TextBlock Text="👥" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="txtEmpleadosActivos" Text="0" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="Empleados activos" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Marcajes hoy -->
            <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="16" BorderBrush="#E2E8F0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <Border Width="46" Height="46" CornerRadius="23" Background="#DBEAFE" VerticalAlignment="Center">
                        <TextBlock Text="🕒" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="txtMarcajesHoy" Text="0" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="Marcajes hoy" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Tardanzas -->
            <Border Grid.Column="4" Background="White" CornerRadius="12" Padding="16" BorderBrush="#E2E8F0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <Border Width="46" Height="46" CornerRadius="23" Background="#FEE2E2" VerticalAlignment="Center">
                        <TextBlock Text="⚠️" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="txtTardanzasHoy" Text="0" FontSize="24" FontWeight="Bold" Foreground="#DC2626"/>
                        <TextBlock Text="Tardanzas hoy" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Por admin -->
            <Border Grid.Column="6" Background="White" CornerRadius="12" Padding="16" BorderBrush="#E2E8F0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <Border Width="46" Height="46" CornerRadius="23" Background="#DCFCE7" VerticalAlignment="Center">
                        <TextBlock Text="📝" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="txtPorAdminHoy" Text="0" FontSize="24" FontWeight="Bold" Foreground="#166534"/>
                        <TextBlock Text="Registros por admin" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ============ TABLA DE MARCAJES DEL DÍA ============ -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Color="#000000" Opacity="0.04"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Filtros -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Mostrar:" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="1" x:Name="cmbFiltro" Width="160" Height="34" FontSize="13" Padding="6,2" SelectedIndex="0" SelectionChanged="cmbFiltro_SelectionChanged">
                        <ComboBoxItem Content="Todos"/>
                        <ComboBoxItem Content="Solo tardanzas"/>
                        <ComboBoxItem Content="Solo por admin"/>
                    </ComboBox>

                    <TextBlock Grid.Column="2" x:Name="txtRegistros" Text="" VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="13" Foreground="#64748B"/>
                </Grid>

                <DataGrid Grid.Row="1"
                          x:Name="dgAsistencias"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#94A3B8"
                          VerticalGridLinesBrush="#CBD5E1"
                          HeadersVisibility="Column"
                          Background="White"
                          RowBackground="#FFFFFF"
                          AlternatingRowBackground="#E2E8F0"
                          BorderBrush="#64748B"
                          BorderThickness="1"
                          RowHeight="38"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#334155"/>
                            <Setter Property="Foreground" Value="#FFFFFF"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="#475569"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,6"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}"
                                                Background="{TemplateBinding Background}"
                                                SnapsToDevicePixels="True">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#1E40AF"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Hora" Binding="{Binding Hora, StringFormat=hh:mm tt}" Width="Auto" MinWidth="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Empleado" Binding="{Binding NombreEmpleado}" Width="Auto" MinWidth="200"/>
                        <DataGridTextColumn Header="Cargo" Binding="{Binding Cargo}" Width="Auto" MinWidth="120"/>

                        <DataGridTemplateColumn Header="Estado" Width="Auto" MinWidth="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="10" Padding="10,3" Margin="0,3" HorizontalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Estado}" Value="A tiempo">
                                                        <Setter Property="Background" Value="#DCFCE7"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Estado}" Value="Tarde">
                                                        <Setter Property="Background" Value="#FEE2E2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Estado}" Value="Por admin">
                                                        <Setter Property="Background" Value="#EFF6FF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="12" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Estado}" Value="A tiempo">
                                                            <Setter Property="Foreground" Value="#166534"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Tarde">
                                                            <Setter Property="Foreground" Value="#DC2626"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Estado}" Value="Por admin">
                                                            <Setter Property="Foreground" Value="#1E40AF"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                            <Run Text="{Binding Estado, Mode=OneWay}"/>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Observación" Binding="{Binding Observacion}" Width="*" MinWidth="220"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- ============ EMPLEADOS QUE AÚN NO MARCAN ============ -->
        <Border Grid.Row="3" Background="White" CornerRadius="12" Padding="16" Margin="0,14,0,0" BorderBrush="#E2E8F0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.04"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock x:Name="txtSinMarcarTitulo" Text="⏳ Empleados que aún no han marcado (0)" FontSize="14" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,10"/>
                <ItemsControl x:Name="itemsSinMarcar">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F1F5F9" CornerRadius="12" Padding="10,6" Margin="0,0,8,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⏳" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                    <TextBlock FontSize="12" FontWeight="SemiBold" Foreground="#334155" Text="{Binding Nombre}"/>
                                    <TextBlock FontSize="12" Foreground="#94A3B8" Text=" — "/>
                                    <TextBlock FontSize="12" Foreground="#64748B" Text="{Binding Cargo}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock x:Name="txtSinMarcarVacio" Text="✅ Todos los empleados activos han marcado hoy." FontSize="13" Foreground="#16A34A" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
